# Serena MCP 客户端配置示例
# 复制此文件为 .env 并根据你的实际情况修改配置
#
# 推荐使用 conda 管理 Python 环境：
#   1. 创建环境: make conda-create
#   2. 激活环境: conda activate serena-client
#   3. 初始化项目: make init
#   4. 安装依赖: make install
# 详细说明参见: docs/CONDA_SETUP.md

# Serena 安装目录的绝对路径（必需）
SERENA_DIR=/path/to/your/serena

# MCP 服务器端口（仅 HTTP 客户端使用，默认 8000）
MCP_SERVER_PORT=8000

# 项目根目录（可选，默认为当前工作目录）
# PROJECT_PATH=/path/to/your/project
